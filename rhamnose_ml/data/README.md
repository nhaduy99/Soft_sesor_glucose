Place intermediate modeling tables here if you create any derived datasets.

Recommended artifacts:

- `matched_rhamnose_training_table.csv`
- `eem_features.parquet`
- `raman_features.parquet`

The starter code reads directly from the enriched inventory and raw CSV files, so this folder is optional.
